<h1><%= @profile.user == current_user ? 'My' : 'User ' + @profile.user.email %> profile</h1>

<br />
<%= @profile.avatar? ? image_tag(@profile.avatar, size: "100x100", alt: "Avatar") : image_tag('man.png', size: "100x100", alt: "Avatar") %>

<br />

<h3>Name:</h3>

<p><%= @profile.full_name || 'n/a'%></p>

<h3>Email:</h3>

<p><%= @profile.user.email %></p>

<h3>Phone Number:</h3>

<p><%= @profile.phone || 'n/a'%></p>

<br />

<% if @profile.user == current_user %>
	<%= link_to "Edit Profile", edit_profile_path, class: "btn btn-lg btn-primary" %>
<% end %>
